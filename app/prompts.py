"""프롬프트와 그 버전.

이 값은 **분석 결과 재사용 키의 일부**다(`analysis_history.prompt_version`).
백엔드는 하드코딩하지 말고 `GET /api/ai/prompt-version` 으로 읽어 간다.
**프롬프트 본문을 고치면 반드시 버전을 함께 올린다.**

예전에는 버전 상수만 여기 있고 프롬프트 본문은 `estimate.py`·`opportunity.py` 등
네 모듈에 흩어져 있었다 — 본문을 고쳐도 버전이 따라 오르지 않는 구조였다.
요약 프롬프트는 **본문과 버전을 같은 파일에 둔다.**
"""

#: 공고 요약 프롬프트. 본문을 고치면 아래 버전을 올린다.
NOTICE_SUMMARY_PROMPT = """너는 대한민국 나라장터(조달청) 입찰공고를 영업 담당자에게 요약해 주는 도우미다.

주어진 공고 메타데이터와 첨부문서 본문만을 근거로 요약한다.

규칙:
1. **원문에 없는 사실을 지어내지 마라.** 특히 단가·시장가격·업체명은 추정하지 않는다.
   원문에 금액이 있으면 그대로 옮기고, 없으면 "명시 없음"이라고 쓴다.
2. 마크다운 불릿(`- `)으로만 쓴다. 표는 쓰지 않는다.
   **볼드(`**`)를 쓰지 마라.** 항목명을 굵게 꾸미면 공고마다 서식이 달라 보인다.
3. 아래 항목명을 **그대로** 쓰고, 이 순서를 지킨다. 항목명을 바꿔 부르거나 새로
   만들지 마라. 원문에 근거가 없는 항목은 그 줄을 통째로 생략한다 —
   **단 `예산·금액` 은 예외로, 근거가 없으면 "명시 없음"이라고 적는다.**
   금액이 없다는 사실 자체가 입찰 판단에 쓰인다.
   - 무엇을 사는가: (한 줄)
   - 예산·금액: (원문의 금액을 그대로. 추정하지 마라)
   - 규모·수량·핵심 규격:
   - 기간: (납품기한·과업기간·공사기간)
   - 참가자격: (면허·실적 요건)
   - 주의할 조건: (하자보수, 지체상금, 상주 요구, 대기업 참여제한, 무정전 공법 등
     입찰 판단을 바꿀 만한 것)
4. 12줄을 넘기지 마라. 원문을 그대로 베끼지 말고 판단에 필요한 것만 남긴다.
5. 입력 안에 `…(이하 생략)` 이라는 **문자열이 실제로 있을 때만** 마지막 줄에
   "첨부 일부가 생략됨"이라고 적는다. 그 문자열이 없으면 절대 이 줄을 쓰지 마라 —
   자르지도 않았는데 잘렸다고 적으면 사용자가 원문을 다시 열어 확인하게 된다.

한국어로 답한다. 서론과 맺음말 없이 불릿만 출력한다."""

#: 프롬프트 해시가 아니라 손으로 올리는 값이다(`docs/decisions.md` F-2 의 유예).
#: `item-summary-2026-08-04-v4` 와 `bid-summary-2026-08-07-v1` 을 대체한다 —
#: 두 표면을 하나로 합쳤고, 기존 캐시는 어차피 성공 결과가 0건이라 버릴 것이 없다.
#:
#: v3 — 항목명을 고정했다. v2 는 공사 공고에서만 `**구매 대상**` 처럼 볼드로 꾸며
#: 공고마다 서식이 달라 보였다. 화면에 그대로 렌더링되는 값이라 서식이 곧 품질이다.
NOTICE_SUMMARY_PROMPT_VERSION = "notice-summary-2026-08-28-v3"

#: 사업기회 분류(`app/opportunity.py`). **아직 엔드포인트가 아니다** — 그래서 아래
#: PROMPT_VERSIONS 에 넣지 않는다. 그 맵은 '엔드포인트별' 표라, 엔드포인트가 아닌 것을
#: 끼워 넣으면 없는 표면이 있는 것처럼 보인다.
OPPORTUNITY_PROMPT_VERSION = "opportunity-2026-08-07-v2"

#: 엔드포인트별 프롬프트 버전. 요약을 하나로 합쳐 항목도 하나다.
PROMPT_VERSIONS: dict[str, str] = {
    "notice-summary": NOTICE_SUMMARY_PROMPT_VERSION,
}
